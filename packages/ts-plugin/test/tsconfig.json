{
  "//": "Tests are not part of the build — this config is only for type-checking (IDE + tsc).",
  "//module": "vitest runs tests as ESM — hence esnext/bundler and import.meta.",
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "declaration": false,
    "rootDir": "..",
    "module": "esnext",
    "moduleResolution": "bundler"
  },
  "include": ["**/*.ts"]
}
